name: CI/CD for Sirasa Service

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ sirasa.DOCKER_USERNAME }}
          password: ${{ sirasa.DOCKER_PASSWORD }}

      - name: Build & Push Docker Image
        run: |
          docker build -t ${{ sirasa.DOCKER_USERNAME }}/sirasa-service:latest .
          docker push ${{ sirasa.DOCKER_USERNAME }}/sirasa-service:latest

  deploy:
    needs: build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Deploy & Restart Container via SSH
        uses: appleboy/ssh-action@master
        with:
          host: ${{ sirasa.SSH_HOST }}
          username: ${{ sirasa.SSH_USER }}
          key: ${{ sirasa.SSH_PRIVATE_KEY }}
          script: |
            echo "🔑 Login ke Docker Hub..."
            docker login -u ${{ sirasa.DOCKER_USERNAME }} -p ${{ sirasa.DOCKER_PASSWORD }}

            echo "📥 Menarik Image Terbaru..."
            docker pull ${{ sirasa.DOCKER_USERNAME }}/sirasa-service:latest

            echo "🛑 Menghentikan Container Lama..."
            docker stop sirasa-service || true
            docker rm sirasa-service || true

            echo "🚀 Menjalankan Container Baru..."
            docker run -d --name sirasa-service -p 3000:3000 \
              --network sirasa-network \
              ${{ sirasa.DOCKER_USERNAME }}/sirasa-service:latest
